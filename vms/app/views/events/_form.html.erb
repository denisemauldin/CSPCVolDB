<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :abbv %><br>
    <%= f.text_field :abbv %>
  </div>
  <div class="field">
    <%= f.label :open_time %><br>
    <%= f.time_field :open_time, :ignore_date => true %>
  </div>
  <div class="field">
    <%= f.label :calendar_id %><br>
    <%= f.collection_select :calendar_id, Calendar.order(:name), :id, :name %>
  </div>
  <div class="field">
    <%= f.label :duration %><br>
    <%= f.number_field :duration %>
  </div>
  <div class="field">
    <%= f.label :default_location %><br>
    <%= f.number_field :default_location %>
  </div>
  
  <p>
    <%= f.label :recurring_rule, "Event Recurrence Schedule" %><br/>
    <%= f.select_recurring :recurring_rule, [ ["No recurring", "{}"]], :allow_blank => true %>
  </p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
